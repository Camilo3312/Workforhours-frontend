import React from 'react'
import { NavLink } from 'react-router-dom'

export const Menu = () => {

    return (
        <nav>
            <NavLink to='service/add'>Agregar</NavLink>
        </nav>
    )
}
